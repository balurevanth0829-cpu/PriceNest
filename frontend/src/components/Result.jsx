import React from 'react';
import { Home } from 'lucide-react';

export default function Result({ price, onReset }) {
  // Format the price with Indian numbering system or standard depending on preference.
  // Using standard local string here.
  const formattedPrice = new Intl.NumberFormat('en-IN', {
    style: 'currency',
    currency: 'INR',
    maximumFractionDigits: 0
  }).format(price);

  return (
    <div className="flex flex-col items-center justify-center min-h-[70vh] text-center px-4 animate-in zoom-in-95 duration-500">
      <h2 className="text-2xl md:text-3xl font-bold text-slate-700 mb-6">Your Estimated House Price</h2>
      
      <div className="bg-white px-12 py-8 rounded-3xl shadow-sm border border-brand-light mb-8 relative overflow-hidden">
        <div className="absolute top-0 left-0 w-full h-2 bg-gradient-to-r from-brand-light via-brand to-brand-dark"></div>
        <div className="text-5xl md:text-7xl font-extrabold text-brand-dark tracking-tight">
          {formattedPrice}
        </div>
      </div>
      
      <p className="text-slate-600 mb-4 max-w-md leading-relaxed">
        Estimated using Machine Learning based on the property details you provided.
      </p>
      
      <p className="text-xs text-slate-400 mb-10 max-w-sm">
        Disclaimer: This is an estimated value generated by the machine-learning model and should not be considered an official property valuation.
      </p>
      
      <div className="flex flex-col sm:flex-row gap-4 w-full justify-center max-w-md">
        <button 
          onClick={() => onReset('form')}
          className="px-8 py-3 bg-brand text-white font-medium rounded-xl shadow hover:bg-brand-dark transition-colors"
        >
          Predict Again
        </button>
        <button 
          onClick={() => onReset('landing')}
          className="px-8 py-3 bg-slate-100 text-slate-700 font-medium rounded-xl shadow-sm hover:bg-slate-200 transition-colors flex items-center justify-center gap-2"
        >
          <Home className="w-4 h-4" />
          Back to Home
        </button>
      </div>
    </div>
  );
}
